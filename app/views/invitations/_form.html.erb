<%= form_for([@event, @invitation]) do |f| %>
  <% if @invitation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invitation.errors.count, "error") %> prohibited this invitation from being saved:</h2>

      <ul>
      <% @invitation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.fields_for :user do |nested_form| %>
    <div class="field">
      <%= nested_form.label :username, "Name" %>
      <%= nested_form.text_field :username %>
    </div>
    <div class="field">
      <%= nested_form.label :email, "Email" %>
      <%= nested_form.text_field :email %>
    </div>
    <div class="field">
      <%= nested_form.label :phone, "Phone" %>
      <%= nested_form.text_field :phone %>
    </div>
  <% end %>
  <%= f.hidden_field :event_id %>
  <div class="actions">
    <%= f.submit :class => 'btn btn-primary' %>
  </div>
<% end %>
